# API Configuration
API_HOST=127.0.0.1
API_PORT=8000
UI_ORIGIN=http://localhost:3000
# CRITICAL: Must set a strong API key (64+ chars) for production security
API_KEY=WILL_BE_GENERATED_DURING_DEPLOYMENT

# Database
DATABASE_URL=sqlite+aiosqlite:///./xdr.db

# Detection Configuration
FAIL_WINDOW_SECONDS=60
FAIL_THRESHOLD=6
AUTO_CONTAIN=false

# Honeypot Configuration - T-Pot Integration
HONEYPOT_HOST=localhost
HONEYPOT_USER=luxieum
HONEYPOT_SSH_KEY=~/.ssh/id_rsa
HONEYPOT_SSH_PORT=64295

# T-Pot Honeypot Integration - PRODUCTION VALUES
TPOT_HOST=24.11.0.176
TPOT_SSH_PORT=64295
TPOT_WEB_PORT=64297
TPOT_API_KEY=YOUR_TPOT_SSH_PASSWORD

# LLM Configuration
LLM_PROVIDER=openai
OPENAI_API_KEY=CONFIGURE_IN_AWS_SECRETS_MANAGER
OPENAI_MODEL=gpt-4o-mini
XAI_API_KEY=CONFIGURE_IN_AWS_SECRETS_MANAGER
XAI_MODEL=grok-beta

ABUSEIPDB_API_KEY=CONFIGURE_IN_AWS_SECRETS_MANAGER
VIRUSTOTAL_API_KEY=CONFIGURE_IN_AWS_SECRETS_MANAGER

# Agent Credential Defaults (copy values from scripts/mint_agent_cred.py output)
MINIXDR_AGENT_PROFILE=HUNTER
MINIXDR_AGENT_DEVICE_ID=your-agent-device-id
MINIXDR_AGENT_HMAC_KEY=sha256-of-agent-secret
# Optional bearer token fallback for endpoints still accepting API keys
MINIXDR_AGENT_BEARER=

# Additional agent profiles (optional)
# HUNTER_AGENT_DEVICE_ID=changeme-device-id
# HUNTER_AGENT_HMAC_KEY=changeme-hmac-key
# HUNTER_AGENT_PUBLIC_ID=changeme-public-id

# SECURITY NOTES:
# 1. API_KEY is REQUIRED and must be 64+ characters
# 2. Generate unique keys for each environment 
# 3. Never commit real keys to git
# 4. Rotate keys monthly
# 5. Use AWS Secrets Manager in production (SECRETS_MANAGER_ENABLED=true)
# 6. All keys marked "CONFIGURE_IN_AWS_SECRETS_MANAGER" should be stored in AWS Secrets Manager
# 7. This is a TEMPLATE file - do not put real credentials here
