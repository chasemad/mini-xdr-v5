fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.35
alembic==1.13.3
aiosqlite==0.19.0
# asyncpg==0.29.0  # Commented out - only needed for PostgreSQL in production
pydantic-settings==2.4.0
paramiko==3.3.1
apscheduler==3.10.4
openai==1.58.1
requests==2.31.0
python-multipart==0.0.6
greenlet

# Enhanced ML and AI Agent Dependencies
torch==2.6.0
torchvision==0.21.0
scikit-learn==1.5.2
xgboost==2.1.3
pandas==2.2.3
numpy>=2.1.0

# AI Agents and LangChain
langchain==0.3.11
langchain-openai==0.2.11
langchain-community==0.3.11

# Policy Engine and Configuration
pyyaml==6.0.2

# Security and Cryptography
cryptography==42.0.8

# Additional ML utilities
joblib==1.4.2
matplotlib==3.9.4
seaborn==0.13.2
# scipy==1.13.1  # Temporarily disabled due to macOS compilation issues

# Async HTTP for agent communication
aiohttp==3.9.5
aiofiles==23.2.1

# Prometheus monitoring
prometheus-client==0.20.0

# WebSocket support for real-time agent communication
websockets==12.0

# Phase 2A: Federated Learning Dependencies
tensorflow==2.20.0  # Latest stable version
# tensorflow-federated - Removed due to Python 3.13 compatibility issues
# We implement custom federated learning with TensorFlow + our crypto protocols
pycryptodome==3.19.0  # Enhanced cryptography for secure aggregation

# Phase 2B: Advanced ML & Explainable AI Dependencies
shap==0.46.0  # SHAP explanations for model interpretability (NumPy 2.0 compatible)
lime>=0.2.0.1  # LIME explanations for local interpretability
optuna==3.6.1  # Hyperparameter optimization for ensemble models
scipy>=1.14.0  # Statistical functions for drift detection

# Phase 3: Distributed MCP Architecture Dependencies
kafka-python==2.0.2  # Apache Kafka integration for distributed messaging
aiokafka==0.12.0  # Async Kafka client for high-performance messaging
redis==5.0.1  # Redis client for distributed state management
aioredis==2.0.1  # Async Redis client for caching and sessions
python-consul==1.1.0  # Consul integration for service discovery
pydantic-settings==2.4.0  # Enhanced configuration management
python-jose[cryptography]==3.3.0  # JWT authentication and encryption
